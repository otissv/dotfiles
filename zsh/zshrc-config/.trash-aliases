if [[ "$OS" == "darwin" ]]; then
  function rm () { trash "$@" }
  function rs () { trash "$@" }
  function rl () { trash -l }
elif [[ "$OS" == "linux" ]]; then
  function rm () { trash-put }
  function rs () { trash-restore }
  function rl () { trash-list }
fi
